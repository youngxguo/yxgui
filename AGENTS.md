# AGENTS

Minimal React 19 + TypeScript library using Vite, Vitest, ESLint, Prettier, and `pnpm@9.15.2`.

- Keep the setup simple.
- Do not hand-edit generated output such as `dist/`.
- Use scripts that exist in `package.json`.
- Run `pnpm check:quality` before considering changes done.
